FROM node:20-alpine

WORKDIR /app

COPY frontend/package*.json ./

RUN npm ci

# Source is bind-mounted at runtime; polling is needed for HMR inside Docker.
ENV VITE_USE_POLLING=true

CMD ["npm", "run", "dev"]
